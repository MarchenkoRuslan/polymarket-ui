{
  "name": "polymarket-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve public -l ${PORT:-3000} -s",
    "dev": "serve public -l 3000 -s"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
