# Port for the static file server (default: 3000)
# Railway sets this automatically
PORT=3000

# API base URL (optional — for CI/CD build-time substitution)
# At runtime you can also override via window.__POLYMARKET_CONFIG__ in index.html
API_BASE_URL=https://polymarket-predictor.up.railway.app/api/v1
